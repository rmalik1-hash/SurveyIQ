export default function Hero() {
  return (
    <section className="hero">
      <span className="badge">
        <span className="badge-dot" aria-hidden="true" />
        SAIL AI Innovation Fellowship · IMSA
      </span>

      <h1 className="hero-title">
        Survey data quality,<br />explained.
      </h1>

      <p className="hero-sub">
        Upload a survey export. SurveyIQ finds the respondents who answered carelessly and
        writes a plain-English reason for every flag — so you can decide what to trust.
      </p>
    </section>
  );
}
