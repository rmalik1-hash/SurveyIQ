__pycache__/
*.pyc
data/synthetic/*.csv
data/synthetic/*.json

# Real survey data must never be committed (contains names/emails)
*.xlsx
data/real/

# Aggregate-only analysis history (regenerable, local to each install)
data/history.json
